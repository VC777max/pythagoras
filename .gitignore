# Dependency directories
**/node_modules/

# Production build directories (we keep padel-server/dist to allow easy deployment without building on server)
# padel-web/dist is ignored as it builds into padel-server/dist
padel-web/dist/

# SQLite databases and temp files
*.db
*.db-journal
*.db-shm
*.db-wal
padel-server/padel_planner.db*

# Android build outputs and caches
.gradle/
**/build/
**/local.properties
.kotlin/

# IDE files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# OS files
Thumbs.db
.DS_Store
